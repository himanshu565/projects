export const userWritingListener = (userId: string, caretPosition: number) => {
    //TODO: This should add a caret at the caretPositon 
    console.log(`user ${userId} is writing at position ${caretPosition}`);
}
